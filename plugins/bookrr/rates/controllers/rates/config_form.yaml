# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: Rate

# Model Form Field configuration
form: $/aeroparks/rates/models/rate/fields.yaml

# Model Class name
modelClass: Bookrr\Rates\Models\Rate

# Default redirect location
defaultRedirect: aeroparks/rates/rates

# Create page
create:
    title: Create Rate
    redirect: aeroparks/rates/rates/update/:id
    redirectClose: aeroparks/rates/rates

# Update page
update:
    title: Edit Rate
    redirect: aeroparks/rates/rates
    redirectClose: aeroparks/rates/rates

# Preview page
preview:
    title: Preview Rate
